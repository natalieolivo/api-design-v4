import prisma from "../db";

export const getUpdatePoints = async (req, res) => {
  // const product
  // const updates
  // then reduce updates to get products?
  // or query directly, but even if I have an index on Updates, how can I pull the points?
  // const updatePoints = prisma.update.findMany({
  //   where: {
  //     belongsToId: req.user.id,
  //   },
  // });
};

// export const createUpdatePoint = (req, res) => {
//     const updatePoint = prisma.updatePoints.create({

//     })
// }
